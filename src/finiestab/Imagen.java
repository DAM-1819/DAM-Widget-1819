package finiestab;

import java.awt.Dimension;
import java.awt.Graphics;
import java.awt.Image;

import javax.swing.JPanel;

public class Imagen extends JPanel{
/*
	Image img;
	public Imagen(Image img) {
		this.img=img;
		Dimension dimension=new Dimension(img.getWidth(null),img.getHeight(null));
		this.setPreferredSize(dimension);
		this.setMinimumSize(dimension);
		this.setMinimumSize(dimension);
		this.setSize(dimension);
		this.setLayout(null);
	}
	@Override
	protected void paintComponent(Graphics g) {
		g.drawImage(img, 0, 0, null);
	}
	*/
	
	
	/**//**//**//**//**//**//**/
	
	
	/*int x, y;

    public Imagen(JPanel titulo) {
        this.x = titulo.getWidth();
        this.y = titulo.getHeight();
        this.setSize(x, y);
    }

    @Override
    public void paint(Graphics g) {
        ImageIcon Img = new ImageIcon(getClass().getResource("recursos_iniesta/titulo.PNG"));
        g.drawImage(Img.getImage(), 0, 0, x, y, null);
    }    
*/
}
